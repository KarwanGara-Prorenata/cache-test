name: Unit Tests Workflow

on:
    repository_dispatch:
        types: [trigger-test]

    push:
        branches:
            - PRNJ-10223-DEV-7606-Migrate-Unit-Testing

permissions:
    contents: write
    
defaults:
    run:
        shell: bash

jobs: 
    unit-tests:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Repository
              uses: actions/checkout@v4

            - name: test
              run: |
                echo "Running unit tests"
                echo "Unit tests passed"